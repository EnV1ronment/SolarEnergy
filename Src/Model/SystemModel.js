export default class SystemModel {

    constructor(
        token,
        firmId,
        stationId,
        userId
    ) {
        this.token = token;
        this.firmId = firmId;
        this.stationId = stationId;
        this.userId = userId;
    }

}